{
  "name": "zama-fhevm-project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run deploy-sepolia.js --network sepolia",
    "deploy:zama": "npx hardhat run scripts/deploy.js --network sepolia",
    "deploy:local": "npx hardhat run scripts/deploy.js --network localhost",
    "start": "cd frontend && node server.js",
    "dev": "cd frontend && node server.js"
  },
  "keywords": ["zama", "fhevm", "confidential", "voting", "blockchain"],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "description": "A confidential voting smart contract using Zama FHEVM",
  "dependencies": {
    "@fhevm/solidity": "^0.7.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.0",
    "ethers": "^6.15.0",
    "fhevm": "^0.6.2",
    "hardhat": "^2.26.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "chai": "^4.5.0"
  }
}
